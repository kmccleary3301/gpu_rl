# Occupancy-Limited Playbook

Low occupancy paired with moderate register pressure or shared-memory use usually means the kernel is leaving latency hiding on the table. Compare launch shape, registers per thread, block size, and whether a smaller tile or staged loop can improve residency.
