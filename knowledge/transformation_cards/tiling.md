# Tiling

Tiling increases locality by loading a working set that can be reused before moving on. For this project, tiling is the default first transformation to consider in reductions, routing, and attention-adjacent kernels.
