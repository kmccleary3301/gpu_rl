# Vectorization

Vectorization widens loads or stores so the compiler and hardware can move more data per instruction. It is most useful when alignment is stable and control flow is simple enough that wider memory operations do not increase masking waste.
