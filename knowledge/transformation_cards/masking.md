# Masking

Masking controls boundary behavior and correctness in partial tiles, ragged rows, and decode-style gathers. It is also a performance tool because sloppy masks can force extra work or block vectorized fast paths.
