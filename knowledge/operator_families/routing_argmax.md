# Routing Argmax

Routing argmax kernels model stable top-1 expert assignment and highlight tie-breaking, mask handling, and gather-style memory access. They are useful for studying MoE-style routing behavior before moving to fuller top-k or scatter/gather pipelines.
