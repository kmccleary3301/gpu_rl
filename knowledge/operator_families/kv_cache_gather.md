# KV-Cache Gather

KV-cache gather kernels fetch cached rows by page or token index and are a practical bridge from simple routing to attention-serving workloads. They emphasize indexed memory access, masking, and the boundary between memory bandwidth and control-flow overhead.
