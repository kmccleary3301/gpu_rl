# GPU Cockpit Knowledge Layer

This directory holds structured knowledge objects that are intended to be queryable by both humans and future agents.

The first pass favors compact, high-signal markdown over heavyweight ingestion. Each document should map cleanly onto an operator family, profiler playbook, transformation card, benchmark note, or hardware note.
