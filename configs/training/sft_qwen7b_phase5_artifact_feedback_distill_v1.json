{
  "config_id": "cfg/sft/qwen7b/phase5_artifact_feedback_distill/v1",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5_artifact_feedback_distill_train_v1",
      "split": "train",
      "required": true,
      "notes": [
        "Turn-level artifact-conditioned distillation train split from the Phase 5 hard-slice freeze."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5_artifact_feedback_distill_dev_v1",
      "split": "dev",
      "required": true,
      "notes": [
        "Turn-level artifact-conditioned distillation dev split from the Phase 5 hard-slice freeze."
      ]
    }
  ],
  "eval_dataset_refs": [],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 16,
  "output_dir": "artifacts/training/qwen7b_phase5_artifact_feedback_distill_v1",
  "notes": [
    "First Phase 5 artifact-conditioned distillation run on the hard-slice observation/action dataset.",
    "Primary goal is improvement on the tranche-1 hard targets rather than broad optimize coverage."
  ]
}
