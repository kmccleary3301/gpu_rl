{
  "config_id": "cfg/sft/qwen7b/phase5a_root_transition_distill/v1",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5a_root_transition_distill_train_v1",
      "split": "train",
      "required": true,
      "notes": [
        "Focused early-turn root-transition train split from phase5a_hard_trace_freeze_v2."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5a_root_transition_distill_dev_v1",
      "split": "dev",
      "required": true,
      "notes": [
        "Small dev split with KB-v3 official softmax and in-domain routing signal."
      ]
    }
  ],
  "eval_dataset_refs": [],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 24,
  "output_dir": "artifacts/training/qwen7b_phase5a_root_transition_distill_v1",
  "notes": [
    "Focused early-turn learner refresh after broader tranche transfer remained at 0/4 for both distill_v3 and tranche1_transfer_distill_v1.",
    "Hypothesis: the next high-EV move is to teach the bench-to-patch root transition directly, with extra emphasis on routing_argmax_hard and early compare structure."
  ]
}
