{
  "config_id": "cfg/sft/qwen7b/optimize_trace_qlora_pilot/v2_repeat",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/optimize_trace_sft_train_v3",
      "split": "train",
      "required": true,
      "notes": [
        "Repeatability pass on the same optimize-trace v3 SFT split used by pilot v2."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/optimize_trace_sft_dev_v3",
      "split": "dev",
      "required": true,
      "notes": [
        "Same dev split as pilot v2 for repeatability comparison."
      ]
    }
  ],
  "eval_dataset_refs": [
    {
      "dataset_kind": "trajectory",
      "path": "datasets/optimize_trace_transition_dev_v3",
      "split": "dev",
      "required": true,
      "notes": [
        "Same trajectory sanity-check split as pilot v2."
      ]
    }
  ],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 16,
  "output_dir": "artifacts/training/qwen7b_optimize_trace_qlora_pilot_v2_repeat",
  "notes": [
    "Repeatability run for the local optimize-trace v3 pilot.",
    "Used to compare stability of memory, runtime shape, and artifact layout against pilot v2."
  ]
}
