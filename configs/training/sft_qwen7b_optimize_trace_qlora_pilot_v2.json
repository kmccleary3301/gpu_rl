{
  "config_id": "cfg/sft/qwen7b/optimize_trace_qlora_pilot/v2",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/optimize_trace_sft_train_v3",
      "split": "train",
      "required": true,
      "notes": [
        "Expanded optimize-trace v3 SFT split with two-attempt positives."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/optimize_trace_sft_dev_v3",
      "split": "dev",
      "required": true,
      "notes": [
        "Dev split includes harder public softmax-wide near-miss context."
      ]
    }
  ],
  "eval_dataset_refs": [
    {
      "dataset_kind": "trajectory",
      "path": "datasets/optimize_trace_transition_dev_v3",
      "split": "dev",
      "required": true,
      "notes": [
        "Trajectory-format optimize traces for pilot v2 sanity checks."
      ]
    }
  ],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 16,
  "output_dir": "artifacts/training/qwen7b_optimize_trace_qlora_pilot_v2",
  "notes": [
    "Second non-smoke local 7B QLoRA pilot on optimize-trace v3.",
    "Intended to test whether richer two-attempt traces move held-out bounded optimize behavior."
  ]
}
