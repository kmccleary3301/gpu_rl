{
  "config_id": "cfg/sft/qwen7b/optimize_trace_qlora_pilot/v3",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/optimize_trace_sft_train_v4",
      "split": "train",
      "required": true,
      "notes": [
        "Expanded optimize-trace v4 SFT split with the branching row-sum success and final teacher-tranche public traces."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/optimize_trace_sft_dev_v4",
      "split": "dev",
      "required": true,
      "notes": [
        "Dev split keeps harder public positive and usable-negative traces."
      ]
    }
  ],
  "eval_dataset_refs": [
    {
      "dataset_kind": "trajectory",
      "path": "datasets/optimize_trace_transition_dev_v4",
      "split": "dev",
      "required": true,
      "notes": [
        "Trajectory-format optimize traces for pilot v3 sanity checks."
      ]
    }
  ],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 24,
  "output_dir": "artifacts/training/qwen7b_optimize_trace_qlora_pilot_v3",
  "notes": [
    "Third non-smoke local 7B QLoRA pilot on optimize-trace v4.",
    "Intended to test whether deeper branching and final teacher-tranche traces improve held-out optimize behavior."
  ]
}
