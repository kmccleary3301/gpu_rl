{
  "config_id": "cfg/sft/qwen7b/phase5a_artifact_feedback_distill/v2",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5a_artifact_feedback_distill_train_v1",
      "split": "train",
      "required": true,
      "notes": [
        "Turn-level artifact-conditioned distillation train split from the Phase 5A hard-tranche freeze."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5a_artifact_feedback_distill_dev_v1",
      "split": "dev",
      "required": true,
      "notes": [
        "Turn-level artifact-conditioned distillation dev split from the Phase 5A hard-tranche freeze."
      ]
    }
  ],
  "eval_dataset_refs": [],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 24,
  "output_dir": "artifacts/training/qwen7b_phase5a_artifact_feedback_distill_v2",
  "notes": [
    "Second artifact-conditioned distillation run aligned to the cleaned Phase 5A 4/4 hard tranche.",
    "Primary goal is movement on attention_score, softmax_wide, and routing_argmax_hard under the aligned hard eval surface."
  ]
}
