{
  "config_id": "cfg/rl/qwen7b/phase5a_optimize_rwr/branchaware_v1",
  "model_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "tokenizer_id": "Qwen/Qwen2.5-Coder-7B-Instruct",
  "adapter_mode": "qlora",
  "dataset_refs": [
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5a_optimize_rwr_branchaware_train_v1",
      "split": "train",
      "required": true,
      "notes": [
        "Branch-aware reward-weighted regression dataset built from the aligned Phase 5A hard freeze."
      ]
    },
    {
      "dataset_kind": "sft",
      "path": "datasets/phase5a_optimize_rwr_branchaware_dev_v1",
      "split": "dev",
      "required": true,
      "notes": [
        "Held-out non-official dev slice from the same Phase 5A hard freeze."
      ]
    }
  ],
  "eval_dataset_refs": [],
  "context_length": 4096,
  "per_device_batch_size": 1,
  "gradient_accumulation_steps": 8,
  "learning_rate": 2e-05,
  "num_epochs": 1,
  "max_steps": 12,
  "output_dir": "artifacts/training/qwen7b_phase5a_optimize_rwr_branchaware_v1",
  "notes": [
    "First narrow Phase 5A branch-aware RWR comparison on the aligned hard freeze.",
    "Intended as a decision experiment against artifact-distill v2 and teacher-corrected v1, not as broad RL."
  ]
}
