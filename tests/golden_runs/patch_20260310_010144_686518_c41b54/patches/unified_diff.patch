--- a/workloads/reference/triton_attention_score_weak_baseline.py
+++ b/workloads/reference/triton_attention_score_weak_baseline.py
@@ -4,7 +4,6 @@
 import json
 from pathlib import Path
 import sys
-import time
 
 import torch
 
@@ -12,18 +11,13 @@
 if str(HERE) not in sys.path:
     sys.path.insert(0, str(HERE))
 
-from triton_attention_score_shared import (
-    HIDDEN_K,
-    HIDDEN_Q,
-    VISIBLE_K,
-    VISIBLE_Q,
-    reference_attention_tensor,
-)
+from triton_attention_score_kernel import triton_attention_scores
+from triton_attention_score_shared import HIDDEN_K, HIDDEN_Q, VISIBLE_K, VISIBLE_Q
 
 
 def _benchmark_inputs(n_q: int, n_k: int, head_dim: int) -> tuple[torch.Tensor, torch.Tensor]:
-    q_base = torch.arange(n_q * head_dim, dtype=torch.float32)
-    k_base = torch.arange(n_k * head_dim, dtype=torch.float32)
+    q_base = torch.arange(n_q * head_dim, device="cuda", dtype=torch.float32)
+    k_base = torch.arange(n_k * head_dim, device="cuda", dtype=torch.float32)
     q = ((q_base.reshape(n_q, head_dim) * 13) % 257) / 257.0
     k = ((k_base.reshape(n_k, head_dim) * 29) % 251) / 251.0
     return q, k
@@ -32,16 +26,13 @@
 def _run_benchmark(repeats: int) -> None:
     q, k = _benchmark_inputs(n_q=512, n_k=512, head_dim=64)
     for _ in range(repeats):
-        for _baseline_pass in range(48):
-            out = reference_attention_tensor(q, k, causal=True)
-    # Keep the weak baseline decisively slower under subprocess timing, where
-    # interpreter startup dominates small scripted kernels.
-    time.sleep(1.0)
+        out = triton_attention_scores(q, k, causal=True)
+        torch.cuda.synchronize()
     _ = out
 
 
 def _serialize(q: torch.Tensor, k: torch.Tensor) -> list[list[float]]:
-    scores = reference_attention_tensor(q.float(), k.float(), causal=True)
+    scores = triton_attention_scores(q.cuda(), k.cuda(), causal=True).cpu()
     return [[round(float(value), 6) for value in row] for row in scores.tolist()]
 
 
